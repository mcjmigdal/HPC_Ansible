---

- name: Install pip
  apt: 
    name: python-pip
    state: latest
    update_cache: yes
  tags: ansible

- name: Install ansible via pip
  pip:
    name: ansible
    state: latest
  tags: ansible

- name: Install OpenLDAP
  apt:
    name: slapd, ldap-utils
    state: latest
    update_cache: yes
  tags: setup
